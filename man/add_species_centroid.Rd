% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geo_functions.r
\name{add_species_centroid}
\alias{add_species_centroid}
\title{Add a species with a country or region centroid to a geographic dataset}
\usage{
add_species_centroid(
  df,
  species_name,
  iso3,
  region = NULL,
  col_species = "species",
  col_lon = "lon",
  col_lat = "lat"
)
}
\arguments{
\item{df}{A data frame containing a species column, a latitude column and a
longitude column.}

\item{species_name}{Character. The species name to add.}

\item{iso3}{Character. The ISO3 code of the country in which the species is
present.}

\item{region}{Character or NULL (default NULL). Optional region within the
country (e.g., a province/state). If provided, the function will return
the centroid of the region instead of the whole country.}

\item{col_species}{Character. Name of the column in `df` for species names.}

\item{col_lon}{Character. Name of the column in `df` for longitude values.}

\item{col_lat}{Character. Name of the column in `df` for latitude values.}
}
\value{
A data frame with the new species row appended to the input `df`.
}
\description{
This function adds a single species record to an existing data frame of
geographic occurrences. It uses the `countryref` dataset from the
`CoordinateCleaner` package to obtain the centroid coordinates of a specified
country or region.
}
\details{
This function was designed for when species are missing from a GBIF
dataset and you want to provide a placeholder occurrence at a country/region
centroid.
}
\examples{
\dontrun{
# Add Quercus sagrana in Cuba
df <- add_species_centroid(gbif_df,
                           "Quercus sagrana",
                           "CUB")
}

}
