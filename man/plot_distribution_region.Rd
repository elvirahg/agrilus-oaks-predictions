% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_functions.r
\name{plot_distribution_region}
\alias{plot_distribution_region}
\title{Plot plant species distribution per region (level)}
\usage{
plot_distribution_region(
  df,
  group_level = "LEVEL3_NAM",
  counts_type = "species",
  world_sf = NULL,
  breaks = c(-Inf, 0, 10, 20, 30, 40, Inf),
  labels = c("0", "1–10", "11–20", "21–30", "31–40", "40+"),
  palette_vals = RColorBrewer::brewer.pal(9, "PuBuGn"),
  na_col = "gray70"
)
}
\arguments{
\item{df}{A data frame (or sf object) containing plant distribution data.}

\item{group_level}{Character string specifying the column to group by
(default `"LEVEL3_NAM"`).}

\item{counts_type}{Character string specifying which counts to plot.
  Options are `"sp"`, `"native_sp"`, `"hosts"`, `"hosts_native"`, `"pred"`,
`"native_pred"`. Defaults to `"species"`.}

\item{world_sf}{Optional `sf` object with world polygons. If `NULL`
(default), uses rnaturalearth.}

\item{breaks}{Numeric vector specifying breakpoints for binning counts.}

\item{labels}{Character vector of labels for the bins.}

\item{na_col}{Colour for NA values. Defaults to `"gray70"`.}

\item{palette_val}{Colour palette values for `scale_fill_brewer`; must be a
character vector. Defaults to RColorBrewer::brewer.pal(9, "PuBuGn").}
}
\value{
A ggplot object showing a world map coloured by the selected counts.
}
\description{
This function prepares counts of plant species per region and plots them
on a world map with discrete colour bins.
}
\examples{
plot_distribution_region(oak_distributions, counts_type = "native_sp")

}
\seealso{
\code{\link{prepare_counts_region}}
}
